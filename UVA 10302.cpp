#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(0);
    unsigned long long int n;
    while(cin >> n) cout << n * n * (n + 1) * (n + 1) / 4 << '\n';
}
